[kernel] Parsing tests/rpp/function_pointers/fptr_forall_multiple_vars.c (with preprocessing)
[rpp] ***************************************
[rpp] Rpp start
[rpp] ***************************************
/* Generated by Frama-C */
/*@ assigns \result;
    assigns \result \from a, b; */
int max(int a, int b)
{
  int tmp;
  if (a > b) tmp = a; else tmp = b;
  return tmp;
}

/*@ assigns \result;
    assigns \result \from a, b; */
int min(int a, int b)
{
  int tmp;
  if (a < b) tmp = a; else tmp = b;
  return tmp;
}

/*@
axiomatic Relational_axiome_1 {
  logic int select_acsl_pure_1(int (*fn)(int , int ), int a, int b) ;
  
  lemma Relational_lemma_1{L}:
    ∀ int a1, int a2, int b1, int b2;
      a1 ≡ a2 ⇒
      b1 ≡ b2 ⇒
      select_acsl_pure_1(&max, a1, b1) ≡ select_acsl_pure_1(&max, a2, b2);
  
}

*/
/*@ requires \valid_function(fn);
    assigns \result;
    assigns \result \from a, b, fn;
    
    behavior Relational_behavior_1:
      ensures \result ≡ select_acsl_pure_1(\old(fn), \old(a), \old(b));
 */
int select(int (*fn)(int , int ), int a, int b)
{
  int tmp;
  /*@ calls max, min; */
  {
    tmp = (*fn)(a,b);
    return tmp;
  }
}

/*@ assigns \result;
    assigns \result \from a, b; */
int max_aux_1(int a, int b);

/*@ requires \valid_function(&max);
    requires \valid_function(&max); */
void relational_wrapper_1(int a1, int a2, int b1, int b2)
{
  int (*aux_local_variable_1)(int a, int b);
  int return_variable_relational_1;
  int (*aux_local_variable_2)(int a, int b);
  int return_variable_relational_2;
  aux_local_variable_1 = & max;
  {
    int tmp_1;
    /*@ assert Rpp: \valid_function(aux_local_variable_1); */
    /*@ calls max, min; */
    {
      tmp_1 = max_aux_1(a1,b1);
      return_variable_relational_1 = tmp_1;
    }
  }
  aux_local_variable_2 = & max;
  {
    int tmp_2;
    /*@ assert Rpp: \valid_function(aux_local_variable_2); */
    /*@ calls max, min; */
    {
      tmp_2 = max_aux_1(a2,b2);
      return_variable_relational_2 = tmp_2;
    }
  }
  /*@ check
        Rpp:
          a1 ≡ a2 ⇒
          b1 ≡ b2 ⇒
          return_variable_relational_1 ≡ return_variable_relational_2;
  */
  return;
}


[rpp] ***************************************
[rpp] Rpp end
[rpp] ***************************************
