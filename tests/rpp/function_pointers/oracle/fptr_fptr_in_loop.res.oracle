[kernel] Parsing tests/rpp/function_pointers/fptr_fptr_in_loop.c (with preprocessing)
[rpp] ***************************************
[rpp] Rpp start
[rpp] ***************************************
/* Generated by Frama-C */
/*@ ensures \result ≡ \old(x) + 1;
    assigns \result;
    assigns \result \from x;
 */
int inc(int x)
{
  int __retres;
  __retres = x + 1;
  return __retres;
}

/*@
axiomatic Relational_axiome_1 {
  logic int apply_n_times_acsl_pure_1(int (*fn)(int ), int x, int n) ;
  
  lemma Relational_lemma_1{L}:
    ∀ int x1, int x2, int n1;
      x1 ≡ x2 ⇒
      n1 ≥ 0 ⇒
      apply_n_times_acsl_pure_1(&inc, x1, n1) ≡
      apply_n_times_acsl_pure_1(&inc, x2, n1);
  
}

*/
/*@ requires \valid_function(fn);
    requires n ≥ 0;
    assigns \result;
    assigns \result \from fn, x, n;
    
    behavior Relational_behavior_1:
      ensures
        \result ≡ apply_n_times_acsl_pure_1(\old(fn), \old(x), \old(n));
 */
int apply_n_times(int (*fn)(int ), int x, int n)
{
  int result = x;
  {
    int i = 0;
    /*@ loop invariant 0 ≤ i ≤ n;
        loop assigns i, result;
        loop variant n - i;
    */
    while (i < n) {
      /*@ calls inc; */
      result = (*fn)(result);
      i ++;
    }
  }
  return result;
}

/*@ ensures \result ≡ \old(x) + 1;
    assigns \result;
    assigns \result \from x;
 */
int inc_aux_1(int x);

/*@ requires \valid_function(&inc);
    requires n1 ≥ 0;
    requires \valid_function(&inc);
    requires n1 ≥ 0;
 */
void relational_wrapper_1(int x1, int x2, int n1)
{
  int (*aux_local_variable_1)(int x);
  int return_variable_relational_1;
  int (*aux_local_variable_2)(int x);
  int return_variable_relational_2;
  aux_local_variable_1 = & inc;
  {
    /*@ assert Rpp: \valid_function(aux_local_variable_1); */
    /*@ assert Rpp: n1 ≥ 0; */
    int result_1 = x1;
    {
      int i_1 = 0;
      /*@ loop invariant 0 ≤ i_1 ≤ n1;
          loop assigns i_1, result_1;
          loop variant n1 - i_1;
      */
      while (i_1 < n1) {
        /*@ calls inc; */
        result_1 = inc_aux_1(result_1);
        i_1 ++;
      }
    }
    return_variable_relational_1 = result_1;
  }
  aux_local_variable_2 = & inc;
  {
    /*@ assert Rpp: \valid_function(aux_local_variable_2); */
    /*@ assert Rpp: n1 ≥ 0; */
    int result_2 = x2;
    {
      int i_2 = 0;
      /*@ loop invariant 0 ≤ i_2 ≤ n1;
          loop assigns i_2, result_2;
          loop variant n1 - i_2;
      */
      while (i_2 < n1) {
        /*@ calls inc; */
        result_2 = inc_aux_1(result_2);
        i_2 ++;
      }
    }
    return_variable_relational_2 = result_2;
  }
  /*@ check
        Rpp:
          x1 ≡ x2 ⇒
          n1 ≥ 0 ⇒
          return_variable_relational_1 ≡ return_variable_relational_2;
  */
  return;
}


[rpp] ***************************************
[rpp] Rpp end
[rpp] ***************************************
