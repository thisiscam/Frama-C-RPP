[kernel] Parsing tests/rpp/behavior_basic.c (with preprocessing)
[rpp] ***************************************
[rpp] Rpp start
[rpp] ***************************************
/* Generated by Frama-C */
/*@
axiomatic Relational_axiome_1 {
  logic int abs_val_acsl_pure_1(int x) ;
  
  logic int abs_val2_acsl_pure_1(int x) ;
  
  lemma Relational_lemma_1{L}:
    ∀ int x; abs_val_acsl_pure_1(x) ≡ abs_val2_acsl_pure_1(x);
  
}
 */
/*@ ensures \result ≥ 0;
    assigns \result;
    assigns \result \from x;
    
    behavior positive:
      assumes x > 0;
      ensures \result ≡ \old(x);
    
    behavior negative:
      assumes x ≤ 0;
      ensures \result ≡ -\old(x);
    
    behavior Relational_behavior_1:
      ensures \result ≡ abs_val_acsl_pure_1(\old(x));
 */
int abs_val(int x)
{
  int tmp;
  if (x > 0) tmp = x; else tmp = - x;
  return tmp;
}

/*@ ensures \result ≥ 0;
    assigns \result;
    assigns \result \from x;
    
    behavior positive:
      assumes x ≥ 0;
      ensures \result ≡ \old(x);
    
    behavior negative:
      assumes x < 0;
      ensures \result ≡ -\old(x);
    
    behavior Relational_behavior_1:
      ensures \result ≡ abs_val2_acsl_pure_1(\old(x));
 */
int abs_val2(int x)
{
  int tmp;
  if (x >= 0) tmp = x; else tmp = - x;
  return tmp;
}

void relational_wrapper_1(int x)
{
  int return_variable_relational_1;
  int return_variable_relational_2;
  {
    int tmp_1;
    if (x > 0) tmp_1 = x; else tmp_1 = - x;
    return_variable_relational_1 = tmp_1;
  }
  {
    int tmp_2;
    if (x >= 0) tmp_2 = x; else tmp_2 = - x;
    return_variable_relational_2 = tmp_2;
  }
  /*@ check Rpp: return_variable_relational_1 ≡ return_variable_relational_2;
  */
  return;
}


[rpp] ***************************************
[rpp] Rpp end
[rpp] ***************************************
